# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db
*.log

# Environment variables
.env
.env.local
.env.*.local

# Database (backup only, keep main)
*.db-journal
*.db.backup

# GPX cache (too large, regenerate from source)
gpx/
geojson_cache/

# Temporary files
*.tmp
*.temp
*.cache
temp/
.claude/

# Build artifacts
*.pyc
*.pyo
.coverage

# Ignore sensitive files
secrets.json
credentials.json
tokens.json

# Docker (rebuild from dockerfile)
# Keep docker-compose.yml and Dockerfile in repo
.dockerignore

# Node modules (if ever added)
node_modules/
package-lock.json
yarn.lock

# Exclude archives (keep production code)
archives/backups/
archives/patches/
archives/deprecated/
archives/data_obsolete/

# Keep but don't track changes
data/color_communes.db.local
*.db.local
